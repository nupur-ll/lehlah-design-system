// Group-only re-export stub for design-sync — see .design-sync/NOTES.md
// ("Grouping" section). Never imported by the app; points cfg.componentSrcMap
// at a directory named after the DS pane group, since the converter can't
// read `@category` off an `export default function Name` declaration.
export { default, type BrandLogoProps, type BrandLogoSize } from "../../../src/components/BrandLogo";
